{% extends 'ADCoreBundle:menudroite.html.twig' %}

{% block content %}
<h1>Ajouter un forfait</h1>

{% if message %}
<p>{{ message }}</p>
{% endif %}

{% endblock %}


{% block body %}
    <h1>Ajouter/Modifier Un forfait </h1>
	 <form action="" method="post" {{ form_enctype(form) }} class="blogger">
		{{ form_errors(form) }}
		{{ form_widget(form) }}

		{{ form_rest(form) }}

        <input type="submit" value="Envoyer" />
    </form>
<br/><br/><br/>
    <ul>
        <li>
            <p><a href="{{ path('ad_forfait_lister') }}">Retour à la liste des forfaits</a></p>
        </li>
    </ul>
{% endblock %}